Surgical patch only.

Replace:
- src/components/ReaderOverlay.jsx

What changed:
- fixed reader layout so the image area and metadata panel stop fighting each other
- anchored controls in a proper top bar
- made the image render in a centered scrolling stage
- moved page info and page list into a stable right-hand panel on desktop
- keeps keyboard navigation and existing props intact
